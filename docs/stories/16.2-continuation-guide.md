# Story 16.2 Continuation Guide

**Status**: 33% Complete - Requires Continuation
**Architecture**: 2-Process Sharded Design
**Estimated Remaining Effort**: 8-12 hours

## Quick Start

### What's Been Completed (6/18 tasks)
1. ✅ **Foundation Architecture**: 2-process sharding design to avoid 500KB limit
2. ✅ **trainer-data-engine.lua**: Data repository with 255 trainer configs embedded
3. ✅ **trainer-encounter-engine.lua**: Logic engine with core algorithms
4. ✅ **Data Extraction Automation**: `scripts/extract-trainer-data.cjs` (96KB data generated)
5. ✅ **Core Algorithms**: Level calculation, matchup scoring, AI switch logic, rewards, encounter probability
6. ✅ **ADP v1.0 Compliance**: Both processes have Info handlers

### What Needs Completion (12/18 tasks)

#### Priority 1: Data Completion (BLOCKING)
**Task 2A - Fix Data Extraction**
1. **Signature Species** (0/50+ extracted):
   - File: `typescript-reference/src/data/balance/signature-species.ts`
   - Issue: Uses `Proxy` wrapper - extraction regex failed
   - Fix: Update `extractSignatureSpecies()` in `scripts/extract-trainer-data.cjs`
   - Format: Keys are trainer names (not TrainerType.X), wrapped in Proxy at line 19-102

2. **Biome Trainer Pools** (0/33+ extracted):
   - Issue: Not found in `typescript-reference/src/data/balance/biomes.ts`
   - Action: Search codebase for trainer pool definitions
   - Possible locations: `trainer-config.ts`, separate pool files
   - Format: Per-biome common/rare/superRare trainer arrays

**Task 2B - Data Integration**
- Implement async message passing between trainer-data-engine ↔ trainer-encounter-engine
- Add error handling for data lookup failures
- Test data retrieval workflows

#### Priority 2: Complete Logic Systems
**Task 4 - Species Selection System**
- Integrate with species-lookup process (external dependency)
- Implement duplicate prevention with evolution chains
- Add type-balanced party validation
- Test species pool tier distribution (512-weighted)

**Task 5 - Party Generation Handler**
- Implement `GeneratePartyMember` handler
- Integrate species selection with data lookups
- Add gender, ability, nature, IV generation
- Implement moveset generation (requires move-lookup process)

**Task 6 - AI Configuration System**
- Extract AI configs from trainer-config.ts
- Implement Tera AI mode logic (NO_TERA, INSTANT_TERA, SMART_TERA)
- Add instant Tera party index selection

**Task 11 - Fixed Trainer Encounters**
- Implement fixed trainer wave mapping (gym leaders at waves 20, 30, 40, etc.)
- Add game mode-specific logic (classic, daily, etc.)
- Extract ClassicFixedBossWaves from TypeScript

**Task 12 - Main Generation Handler**
- Implement `GenerateTrainer` orchestration handler
- Integrate all subsystems (levels, species, party, AI, rewards)
- Add comprehensive error handling
- Return complete trainer data structure

**Task 13 - Validation Handlers**
- Complete `ValidateTrainerType` with biome compatibility
- Add wave-trainer eligibility validation
- Implement trainer data sanity checks

**Task 14 - Dialogue Integration**
- Embed dialogue message keys (encounter/victory/defeat)
- Reference external localization process
- Add variant-specific dialogue selection

#### Priority 3: Testing & Validation
**Task 15 - Unit Testing**
- Create `testing/unit/trainer-data-engine.test.lua`
- Create `testing/unit/trainer-encounter-engine.test.lua`
- Test all algorithms with known inputs/outputs
- Validate data lookup functions

**Task 16 - Integration Testing**
- Create `testing/integration/trainer-encounter-engine-integration.test.lua`
- Test complete trainer generation workflows
- Test inter-process communication (data-engine ↔ encounter-engine)
- Test coordinator orchestration

**Task 17 - Parity Testing**
- Create `testing/parity/trainer-encounter-parity.test.lua`
- Compare level calculations against TypeScript
- Validate species selection with same seed
- Test party generation produces identical results
- Verify matchup scoring matches TypeScript

**Task 18 - Compliance & Optimization**
- Run `npm run lint:ao-sandbox` (AO compliance validation)
- Run `npm run validate:size` (both processes must be <500KB)
- Optimize process sizes if needed
- Performance testing within 5-second timeout

## File Structure

### Production Files
```
processes/
├── trainer-data-engine.lua          # Data repository (946 lines, ~35KB)
├── trainer-encounter-engine.lua     # Logic engine (542 lines, ~20KB)
└── generated/                       # Auto-generated data
    ├── trainer-type-enum.lua        # 255 trainer types
    ├── trainer-configs.lua          # 255 configs (96KB)
    ├── signature-species.lua        # EMPTY - needs fix
    ├── biome-trainer-pools.lua      # EMPTY - needs fix
    └── trainer-data-combined.lua    # Combined output
```

### Tooling
```
scripts/
└── extract-trainer-data.cjs         # Data extraction automation (422 lines)
```

### Testing (To Be Created)
```
testing/
├── unit/
│   ├── trainer-data-engine.test.lua
│   └── trainer-encounter-engine.test.lua
├── integration/
│   └── trainer-encounter-engine-integration.test.lua
└── parity/
    └── trainer-encounter-parity.test.lua
```

## Critical Blocking Issues

### 1. Signature Species Extraction
**Problem**: TypeScript uses `Proxy` wrapper that regex doesn't match
**File**: `typescript-reference/src/data/balance/signature-species.ts:19-102`
**Format**:
```typescript
export const signatureSpecies: SignatureSpecies = new Proxy({
  BROCK: [SpeciesId.ONIX, SpeciesId.GEODUDE, [SpeciesId.OMANYTE, SpeciesId.KABUTO], SpeciesId.AERODACTYL],
  MISTY: [SpeciesId.STARYU, SpeciesId.PSYDUCK, SpeciesId.WOOPER, SpeciesId.LAPRAS],
  // ... 50+ more entries
}, {
  get(target, prop: string) {
    return target[prop as keyof SignatureSpecies] ?? [];
  }
});
```

**Fix Required**:
```javascript
// In scripts/extract-trainer-data.cjs
function extractSignatureSpecies(content) {
    // Match content between "new Proxy({" and "}, {"
    const proxyMatch = content.match(/new Proxy\(\{([^}]+(?:\{[^}]+\}[^}]*)*)\},\s*\{/s);
    if (!proxyMatch) return null;

    const objectBody = proxyMatch[1];
    const trainerMatches = objectBody.matchAll(/([A-Z_0-9]+):\s*\[([^\]]+)\]/g);

    // Process matches...
}
```

### 2. Biome Trainer Pools Missing
**Problem**: Not found in expected file `typescript-reference/src/data/balance/biomes.ts`
**Action**: Search codebase for trainer pool definitions
**Search Command**:
```bash
grep -r "trainerPool\|commonPool\|rarePool" typescript-reference/src/data/ | grep -v node_modules
```

### 3. Type Effectiveness Chart
**Problem**: Matchup score calculations need type chart
**Status**: Placeholder implementation only
**Required**: Embed type effectiveness matrix in trainer-encounter-engine.lua
**Source**: Look for `getTypeDamageMultiplier` in TypeScript

### 4. Species Lookup Integration
**Problem**: Party generation requires external species-lookup process
**Status**: Process doesn't exist yet (different story)
**Workaround**: Use mock species data for initial testing
**Future**: Replace mocks with actual ao.send() calls to species-lookup process

### 5. No Testing
**Problem**: All algorithms UNTESTED
**Impact**: No validation of correctness
**Priority**: HIGH - write unit tests before integration

## Architecture Reference

### Process 1: trainer-data-engine.lua
**Purpose**: Data Repository
**Size Target**: ~300-350KB
**Handlers**:
- `Info` - ADP v1.0 self-documentation
- `GetTrainerConfig` - Retrieve config by TrainerType
- `GetSignatureSpecies` - Get signature Pokemon
- `GetBiomeTrainerPool` - Get trainers for biome
- `GetPartyTemplate` - Get template by name
- `ValidateTrainerType` - Check eligibility

**Data Embedded**:
- 255+ TrainerType enum entries
- 255+ TrainerConfig objects
- 50+ Signature species mappings (PENDING)
- 33+ Biome trainer pools (PENDING)
- Party templates (complete)

### Process 2: trainer-encounter-engine.lua
**Purpose**: Logic Engine
**Size Target**: ~100-150KB
**Handlers**:
- `Info` - ADP v1.0 self-documentation
- `CalculatePartyLevels` - Level calculation algorithm
- `CalculateMatchupScore` - AI matchup scoring
- `EvaluateSwitchDecision` - AI switch logic
- `CalculateRewards` - Money and item rewards
- (Future) `GenerateTrainer` - Main orchestration
- (Future) `GeneratePartyMember` - Individual Pokemon generation
- (Future) `SelectTrainerFromBiome` - Random encounter selection

**Algorithms Implemented**:
- ✅ Base level formula: `1 + (wave/2) + (wave/25)^2`
- ✅ Strength multipliers (0.90x - 1.25x)
- ✅ Progressive scaling for weak Pokemon
- ✅ Matchup scoring (defensive, offensive, speed)
- ✅ Switch decision logic (2x/3x thresholds)
- ✅ Money reward calculation
- ✅ Modifier chance calculation
- ✅ Biome pool selection (80/15/5 distribution)

## TypeScript Reference Files

### Trainer Data Sources
- `typescript-reference/src/enums/trainer-type.ts` - 255 trainer types ✅
- `typescript-reference/src/data/trainers/trainer-config.ts` - 3000+ lines configs ✅ (partial)
- `typescript-reference/src/data/balance/signature-species.ts` - 50+ signatures ⚠️ (needs fix)
- `typescript-reference/src/data/trainers/trainer-party-template.ts` - Party templates ✅
- `typescript-reference/src/data/balance/biomes.ts` - Biome data ⚠️ (pools not found)

### Algorithm Sources
- `typescript-reference/src/field/trainer.ts:47-148` - Trainer generation logic ✅
- `typescript-reference/src/field/trainer.ts:getPartyLevels` - Level calculation ✅
- `typescript-reference/src/field/pokemon.ts:getMatchupScore` - Matchup scoring ⚠️ (placeholder)
- `typescript-reference/src/phases/enemy-command-phase.ts` - Switch logic ✅
- `typescript-reference/src/battle-scene.ts:getWaveMoneyAmount` - Money rewards ✅

## Testing Strategy

### Unit Tests (Priority 1)
1. **Level Calculation** - Test at waves 10, 40, 50, 100, 200
2. **Species Pool Distribution** - 10000 samples, verify 69.5%/24.2%/5.1%/1.0%/0.2%
3. **Matchup Scoring** - All type combinations
4. **Switch Decision** - Various threshold scenarios
5. **Money Rewards** - Key waves with different multipliers
6. **Data Lookups** - All handler functions

### Integration Tests (Priority 2)
1. **Complete Trainer Generation** - Random encounter workflow
2. **Fixed Trainer Generation** - Gym leader workflow
3. **Double Battle Generation** - Tate & Liza workflow
4. **Inter-Process Communication** - Data engine ↔ encounter engine
5. **Coordinator Integration** - Full orchestration

### Parity Tests (Priority 3)
1. **Level Calculations** - Match TypeScript exactly
2. **Species Selection** - Same seed, same results
3. **Party Generation** - Identical teams
4. **Matchup Scoring** - Exact algorithm match
5. **Switch Decisions** - Identical AI behavior

## Development Commands

### Testing
```bash
# Unit tests (aolite)
npm run test:aolite

# Integration tests (aos-local)
npm run test:aos-local

# Parity tests
npm run test:parity

# All tests
npm run test:all
```

### Validation
```bash
# AO compliance
npm run lint:ao-sandbox

# Process size limits
npm run validate:size
```

### Data Extraction
```bash
# Regenerate trainer data
node scripts/extract-trainer-data.cjs

# Output: processes/generated/*.lua
```

## Next Steps Checklist

- [ ] Fix signature species extraction in `scripts/extract-trainer-data.cjs`
- [ ] Locate and extract biome trainer pools
- [ ] Re-run data extraction script
- [ ] Integrate generated data into trainer-data-engine.lua
- [ ] Implement inter-process communication (Task 2B)
- [ ] Complete species selection system (Task 4)
- [ ] Implement party generation handler (Task 5)
- [ ] Add AI configuration system (Task 6)
- [ ] Implement fixed trainer encounters (Task 11)
- [ ] Create main generation handler (Task 12)
- [ ] Complete validation handlers (Task 13)
- [ ] Add dialogue integration (Task 14)
- [ ] Write comprehensive unit tests (Task 15)
- [ ] Write integration tests (Task 16)
- [ ] Write parity tests (Task 17)
- [ ] Run compliance and optimization (Task 18)

## Contact & Context

**Original Developer**: Dev Agent (Claude Sonnet 4.5)
**Session Date**: January 10, 2025
**Story File**: `docs/stories/16.2.story.md`
**Epic**: Epic 16 - World Progression & Biome System

**Key Decisions Made**:
1. Multi-process sharding architecture (2 processes to avoid 500KB limit)
2. Automated data extraction tooling (TypeScript → Lua conversion)
3. ADP v1.0 compliance for both processes
4. Monolithic design within each process (no external requires)

**Quality Standards**:
- 100% AO compliance (no `require()`, no `os.time()`, proper handlers)
- Comprehensive testing (unit, integration, parity)
- Exact behavioral parity with TypeScript
- Process size limits (<500KB each)
- 5-second execution timeout

---

**Good luck with the continuation! The foundation is solid - just needs data completion and thorough testing.** 🚀
